// Leap! Pad — Subproject Declaration
// Tells Gradle that this build contains two subprojects:
//   common/ — shared logic, no loader-specific code
//   fabric/ — Fabric 1.20.1 wiring and API calls
rootProject.name = 'leappad'
include('common')
include('fabric')
